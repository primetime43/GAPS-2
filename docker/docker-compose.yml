version: '3.8'

services:
  gaps2:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "4277:4277"
    volumes:
      - gaps2-data:/app/data
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

volumes:
  gaps2-data:
